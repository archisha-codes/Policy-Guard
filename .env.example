# ============================================================
# POLICYGUARD ENVIRONMENT VARIABLES (OPEN-SOURCE / SELF-HOSTED)
# ============================================================

# --- BACKEND SECRETS ---
ENVIRONMENT=development
USE_SQLITE=true
# DATABASE_URL=postgresql://user:password@host:5432/policyguard
JWT_SECRET=super-secret-jwt-key-change-in-production

# --- OPTIONAL AI ENHANCEMENT (Fallback engine works 100% offline without key) ---
# GOOGLE_API_KEY=your_optional_gemini_api_key

# --- RAG CONFIGURATION (Local self-hosted in-memory vector store used by default) ---
# OPENSEARCH_URL=http://localhost:9200
# OPENSEARCH_ENDPOINT=vpc-endpoint.ap-south-1.es.amazonaws.com

# --- FRONTEND PUBLIC VARIABLES ---
VITE_API_BASE_URL=http://localhost:8000
